services:
  gym-bot:
    build: .
    container_name: gym-bot
    ports:
      - "8000:8000"
    env_file:
      - .env
    restart: unless-stopped
    # Live code reload (dev)
    volumes:
      - ./src:/home/appuser/app/src
      - ./src/prompts:/home/appuser/app/src/prompts
